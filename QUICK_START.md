# 🚀 БЫСТРЫЙ СТАРТ - Пошаговая инструкция

## ⚡ Запуск Backend (5 минут)

### Шаг 1: Откройте PowerShell
Нажмите `Win + X` → выберите "Windows PowerShell" или "Терминал"

### Шаг 2: Перейдите в папку backend
```powershell
cd C:\Users\user\Desktop\aviasales\backend
```

### Шаг 3: Создайте виртуальное окружение
```powershell
python -m venv venv
```

### Шаг 4: Активируйте виртуальное окружение
```powershell
.\venv\Scripts\Activate.ps1
```

**Если появилась ошибка про политику выполнения:**
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```
Затем повторите активацию.

**После активации в начале строки должно появиться `(venv)`**

### Шаг 5: Установите зависимости
```powershell
pip install -r requirements.txt
```

Это займёт 1-2 минуты. Дождитесь завершения.

### Шаг 6: Создайте тестовые данные
```powershell
python seed.py
```

Вы должны увидеть сообщения о создании пользователей, аэропортов, самолётов и рейсов.

### Шаг 7: Запустите сервер
```powershell






















cd .\backend\
.\venv\Scripts\activate
uvicorn main:app --reload


cd mobile
flutter run




```

**Вы должны увидеть:**
```
INFO:     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
INFO:     Started reloader process
INFO:     Started server process
INFO:     Waiting for application startup.
INFO:     Application startup complete.
```

### Шаг 8: Проверьте работу
Откройте в браузере: **http://127.0.0.1:8000/docs**

Вы должны увидеть документацию API (Swagger UI).

✅ **Backend запущен!** Оставьте это окно PowerShell открытым.

---

## 📱 Запуск Flutter приложения

### Шаг 1: Откройте НОВОЕ окно PowerShell
(Не закрывайте то, где запущен backend!)

### Шаг 2: Проверьте Flutter
```powershell
flutter --version
```

Если Flutter не установлен:
1. Скачайте: https://flutter.dev/docs/get-started/install/windows
2. Распакуйте в `C:\flutter` (или другую папку)
3. Добавьте в PATH (переменные окружения)
4. Перезапустите PowerShell

### Шаг 3: Перейдите в папку mobile
```powershell
cd C:\Users\user\Desktop\aviasales\mobile
```

### Шаг 4: Установите зависимости
```powershell
flutter pub get
```

### Шаг 5: Настройте URL (если нужно)

Откройте файл `mobile/lib/api_service.dart` и проверьте строку 10:

```dart
static const String baseUrl = 'http://10.0.2.2:8000';
```

- ✅ Для **эмулятора Android**: оставьте как есть
- 📱 Для **реального телефона**: замените на `http://<ваш-ip>:8000`
  - Узнать IP: выполните `ipconfig` в PowerShell
  - Найдите "IPv4 Address"

### Шаг 6: Запустите эмулятор (если ещё не запущен)

1. Откройте Android Studio
2. Tools → Device Manager
3. Запустите виртуальное устройство (или подключите реальный телефон)

### Шаг 7: Запустите приложение
```powershell
flutter run
```

Приложение должно запуститься на эмуляторе/телефоне.

---

## 🧪 Тестирование

### 1. Вход в систему

В приложении:
- Email: `passenger@test.com`
- Пароль: `password123`

Нажмите "Войти"

### 2. Заполнение профиля

После входа приложение попросит заполнить профиль:
- Имя: `Иван`
- Фамилия: `Иванов`
- Дата рождения: выберите любую
- Паспорт: `1234567890`
- Телефон: `+79991234567` (необязательно)

### 3. Поиск рейса

1. На главном экране выберите:
   - **Откуда**: `SVO` (Москва)
   - **Куда**: `LED` (Санкт-Петербург)
   - **Дата**: сегодня

2. Нажмите "Найти рейсы"

3. Выберите рейс (например, SU100)

### 4. Выбор места

1. На экране деталей рейса вы увидите карту мест
2. Нажмите на свободное место (зелёное)
3. Место станет синим (выбрано)
4. Нажмите "Забронировать"

### 5. Оплата

1. Выберите способ оплаты (Банковская карта, Apple Pay, Google Pay)
2. Нажмите "Оплатить и забронировать"
3. Оплата пройдёт успешно (это мок)

### 6. Мои поездки

1. Нажмите на иконку самолёта в правом верхнем углу
2. Вы увидите своё бронирование
3. Нажмите "Check-in" для регистрации на рейс

---

## ⚠️ Если что-то не работает

### Backend не запускается

1. **Проверьте Python:**
   ```powershell
   python --version
   ```
   Должно быть 3.10 или выше ✅

2. **Проверьте виртуальное окружение:**
   - В начале строки должно быть `(venv)`
   - Если нет → выполните `.\venv\Scripts\Activate.ps1`

3. **Проверьте зависимости:**
   ```powershell
   pip list
   ```
   Должны быть: fastapi, uvicorn, sqlalchemy и т.д.

4. **Порт занят:**
   - Закройте другое приложение на порту 8000
   - Или используйте другой порт: `uvicorn main:app --reload --port 8001`

### Flutter не подключается к backend

1. **Проверьте, что backend запущен:**
   - Откройте http://127.0.0.1:8000/docs
   - Должна открыться документация

2. **Проверьте URL в api_service.dart:**
   - Для эмулятора: `http://10.0.2.2:8000`
   - Для телефона: `http://<ваш-ip>:8000`

3. **Проверьте сеть:**
   - Телефон и компьютер должны быть в одной Wi-Fi сети
   - Брандмауэр не должен блокировать порт 8000

### Ошибка "Connection refused"

- Backend не запущен → запустите `uvicorn main:app --reload`
- Неправильный URL → проверьте `api_service.dart`
- Разные сети → убедитесь, что устройства в одной сети

---

## 📞 Что делать дальше?

1. ✅ Запустите backend
2. ✅ Запустите Flutter
3. ✅ Протестируйте все функции
4. 📖 Изучите код - он написан просто с комментариями
5. 🔧 Экспериментируйте и добавляйте свои функции!

---

**Удачи! 🚀**

