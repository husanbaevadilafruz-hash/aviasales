# Руководство для STAFF (Сотрудников авиакомпании)

## 📋 Что такое STAFF?

**STAFF** — это сотрудники авиакомпании, которые управляют системой бронирования.

### Ключевые особенности:

- ✅ STAFF **НЕ регистрируются** через публичный API (`/register`)
- ✅ STAFF создаются **ТОЛЬКО** через `seed.py` при старте проекта
- ✅ STAFF входят в систему через обычный `/login` и получают JWT токен
- ✅ Может быть **несколько** STAFF пользователей
- ✅ Каждый STAFF — отдельный пользователь со своим логином и паролем

---

## 🔐 Учётные данные STAFF

После запуска `seed.py` создаются следующие STAFF пользователи:

1. **staff@test.com** / **password123**
2. **staff1@airline.com** / **staff123**

### Как войти:

1. Отправьте POST запрос на `/login`:
   ```json
   {
     "email": "staff1@airline.com",
     "password": "staff123"
   }
   ```

2. Получите JWT токен в ответе

3. Используйте токен в заголовке всех запросов:
   ```
   Authorization: Bearer <ваш_токен>
   ```

---

## 🛠️ Функционал STAFF

### 1. Управление самолётами

**Создание самолёта:**
```
POST /airplanes
Authorization: Bearer <токен>
```

**Что делает:**
- Создаёт новый самолёт в системе
- Указывает модель (например, "Boeing 737")
- Определяет все места в самолёте (seat map)
- Система автоматически создаёт все места в базе данных

**Пример запроса:**
```json
{
  "model": "Boeing 737",
  "seats": [
    {"seat_number": "1A"},
    {"seat_number": "1B"},
    {"seat_number": "1C"},
    {"seat_number": "1D"},
    {"seat_number": "2A"},
    ...
  ]
}
```

**Зачем это нужно:**
Перед созданием рейса нужно создать самолёт, который будет использоваться на этом рейсе.

---

### 2. Управление рейсами

#### Создание рейса:
```
POST /flights
Authorization: Bearer <токен>
```

**Что делает:**
- Создаёт новый рейс в системе
- Указывает номер рейса (например, "SU100")
- Выбирает аэропорты отправления и прибытия
- Устанавливает дату и время вылета/прилёта
- Назначает самолёт для рейса
- Устанавливает базовую цену билета

**Пример запроса:**
```json
{
  "flight_number": "SU100",
  "departure_airport_id": 1,
  "arrival_airport_id": 2,
  "departure_time": "2025-12-30T10:00:00",
  "arrival_time": "2025-12-30T11:30:00",
  "airplane_id": 1,
  "base_price": 5000.0
}
```

**Зачем это нужно:**
Рейсы — это основа системы. Без рейсов пассажиры не смогут ничего забронировать.

---

#### Обновление статуса рейса:
```
PATCH /flights/{flight_id}
Authorization: Bearer <токен>
```

**Что делает:**
STAFF может изменять статус рейса для информирования пассажиров.

**Доступные статусы:**
- `SCHEDULED` - Запланирован (рейс создан, но ещё не начался)
- `DELAYED` - Задержан (рейс отложен)
- `BOARDING` - Идёт посадка (пассажиры садятся в самолёт)
- `DEPARTED` - Вылетел (самолёт улетел)
- `ARRIVED` - Прибыл (самолёт приземлился)
- `CANCELLED` - Отменён (рейс отменён)
- `COMPLETED` - Завершён (рейс полностью завершён)

**Пример запроса:**
```json
{
  "status": "DELAYED"
}
```

**Зачем это нужно:**
Пассажиры видят актуальный статус своего рейса в приложении.

---

### 3. Просмотр бронирований

#### Все бронирования:
```
GET /bookings/all
Authorization: Bearer <токен>
```

**Что делает:**
Возвращает **ВСЕ** бронирования в системе.

**Зачем это нужно:**
- Контроль загрузки рейсов
- Просмотр информации о пассажирах
- Аналитика и отчётность

**ВАЖНО:** STAFF **НЕ может** изменять или отменять чужие бронирования через этот эндпоинт.

---

#### Бронирования по рейсу:
```
GET /flights/{flight_id}/bookings
Authorization: Bearer <токен>
```

**Что делает:**
Возвращает все бронирования для конкретного рейса.

**Зачем это нужно:**
- Проверка загрузки конкретного рейса
- Просмотр списка пассажиров на рейс
- Подготовка списка для регистрации

**Пример использования:**
```
GET /flights/1/bookings
```
Вернёт все бронирования для рейса с ID=1.

---

### 4. Объявления (Announcements)

#### Создание объявления:
```
POST /announcements
Authorization: Bearer <токен>
```

**Что делает:**
STAFF создаёт объявление для пассажиров.

**Пример запроса:**
```json
{
  "title": "Задержка рейса SU100",
  "content": "Рейс SU100 задержан на 30 минут. Приносим извинения за неудобства."
}
```

**Зачем это нужно:**
- Информация о задержках рейсов
- Важные уведомления
- Изменения в расписании
- Общая информация для всех пассажиров

**ВАЖНО:** Объявления видны **всем** пользователям (и пассажирам, и staff).

---

## 🔒 Ограничения доступа

### Что STAFF **МОЖЕТ**:
✅ Создавать самолёты  
✅ Создавать рейсы  
✅ Изменять статус рейсов  
✅ Просматривать все бронирования  
✅ Создавать объявления  
✅ Просматривать аэропорты  

### Что STAFF **НЕ МОЖЕТ**:
❌ Регистрироваться через `/register`  
❌ Бронировать билеты (это для пассажиров)  
❌ Оплачивать бронирования  
❌ Делать check-in  
❌ Изменять или отменять чужие бронирования  

---

## 🛡️ Защита эндпоинтов

Все STAFF эндпоинты защищены двумя уровнями:

1. **JWT авторизация** — проверка, что пользователь залогинен
2. **Проверка роли** — проверка, что `role == STAFF`

### Как это работает:

```python
@app.post("/flights")
def create_flight(
    current_user: User = Depends(get_current_staff),  # ← Проверка роли
    ...
):
    # Только STAFF может попасть сюда
```

Если пассажир попытается вызвать STAFF эндпоинт:
- Получит ошибку **403 Forbidden**
- Сообщение: "Not enough permissions. Staff role required."

---

## 📝 Технические детали

### Создание нового STAFF пользователя:

1. Откройте `backend/seed.py`
2. Добавьте в список `staff_users_data`:
   ```python
   {"email": "staff2@airline.com", "password": "staff456"},
   ```
3. Запустите `python seed.py`

### Структура проверки роли:

```python
# В auth.py
def get_current_staff(current_user: User = Depends(get_current_user)):
    if current_user.role != UserRole.STAFF:
        raise HTTPException(403, "Staff role required")
    return current_user
```

---

## 🧪 Тестирование

### Проверка работы STAFF:

1. **Войдите как STAFF:**
   ```bash
   POST http://127.0.0.1:8000/login
   {
     "email": "staff1@airline.com",
     "password": "staff123"
   }
   ```

2. **Создайте самолёт:**
   ```bash
   POST http://127.0.0.1:8000/airplanes
   Authorization: Bearer <токен>
   ```

3. **Создайте рейс:**
   ```bash
   POST http://127.0.0.1:8000/flights
   Authorization: Bearer <токен>
   ```

4. **Просмотрите бронирования:**
   ```bash
   GET http://127.0.0.1:8000/bookings/all
   Authorization: Bearer <токен>
   ```

---

## ⚠️ Важные замечания

1. **STAFF НЕ регистрируются через API** — только через `seed.py`
2. **Простая система ролей** — только PASSENGER и STAFF, без ADMIN
3. **Без сложных permissions** — простая проверка роли
4. **Понятный код** — все комментарии объясняют ЗАЧЕМ, а не только ЧТО

---

## 📚 Связанные файлы

- `backend/auth.py` — функции проверки ролей
- `backend/main.py` — все STAFF эндпоинты
- `backend/seed.py` — создание STAFF пользователей
- `backend/models.py` — модель User с ролью STAFF

---

**Успешной работы! 🚀**







