# Backend - Airline Booking API

Это backend часть проекта системы бронирования авиабилетов на FastAPI.

## 📋 Что это такое?

Backend - это сервер, который:
- Хранит все данные (пользователи, рейсы, бронирования)
- Обрабатывает запросы от Flutter приложения
- Проверяет права доступа (пассажир или сотрудник)
- Выполняет бизнес-логику (бронирование, оплата, check-in)

## 🛠️ Технологии

- **Python 3.10+** - язык программирования
- **FastAPI** - фреймворк для создания API
- **SQLite** - база данных (простая, хранится в одном файле)
- **SQLAlchemy** - для работы с базой данных
- **JWT** - для аутентификации (токены доступа)
- **Pydantic** - для валидации данных

## 📦 Установка и запуск

### Шаг 1: Проверьте Python

Откройте терминал (PowerShell) и проверьте версию Python:

```powershell
python --version
```

Должно быть Python 3.10 или выше. Если Python не установлен, скачайте с [python.org](https://www.python.org/downloads/).

### Шаг 2: Перейдите в папку backend

```powershell
cd backend
```

### Шаг 3: Создайте виртуальное окружение (рекомендуется)

Виртуальное окружение - это изолированная среда для проекта. Это как отдельная комната для каждого проекта.

```powershell
python -m venv venv
```

### Шаг 4: Активируйте виртуальное окружение

```powershell
.\venv\Scripts\Activate.ps1
```

Если появилась ошибка про политику выполнения, выполните:

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

После активации в начале строки терминала появится `(venv)`.

### Шаг 5: Установите зависимости

```powershell
pip install -r requirements.txt
```

Это установит все необходимые библиотеки (FastAPI, SQLAlchemy и т.д.).

### Шаг 6: Создайте тестовые данные

```powershell
python seed.py
```

Это создаст:
- Тестовых пользователей (пассажир и сотрудник)
- Аэропорты
- Самолёты с местами
- Рейсы

**Учётные данные для входа:**
- Пассажир: `passenger@test.com` / `password123`
- Сотрудники (STAFF):
  - `staff@test.com` / `password123`
  - `staff1@airline.com` / `staff123`

**ВАЖНО:** STAFF НЕ регистрируются через `/register`. Они создаются только через `seed.py` и входят через обычный `/login`.

Подробнее о функционале STAFF см. `STAFF_GUIDE.md`

### Шаг 7: Запустите сервер

```powershell
uvicorn main:app --reload
```

Сервер запустится на `http://127.0.0.1:8000`

Флаг `--reload` означает, что сервер автоматически перезапустится при изменении кода (удобно для разработки).

### Шаг 8: Откройте документацию API

Откройте в браузере: **http://127.0.0.1:8000/docs**

Здесь вы увидите все эндпоинты и можете протестировать API прямо в браузере!

## 📁 Структура проекта

```
backend/
├── main.py           # Главный файл с всеми эндпоинтами
├── database.py       # Подключение к базе данных
├── models.py         # Модели (таблицы) базы данных
├── schemas.py        # Схемы для валидации данных
├── auth.py           # Аутентификация и авторизация
├── seed.py           # Скрипт для создания тестовых данных
├── requirements.txt  # Список зависимостей
├── README.md         # Этот файл
├── STAFF_GUIDE.md    # Руководство для STAFF (сотрудников)
└── aviasales.db      # База данных SQLite (создаётся автоматически)
```

## 🔌 Основные эндпоинты

### Аутентификация
- `POST /register` - Регистрация
- `POST /login` - Вход

### Для пассажиров
- `POST /passenger/profile` - Создание/обновление профиля
- `GET /passenger/profile` - Получение профиля
- `GET /flights/search` - Поиск рейсов
- `GET /flights/{id}` - Детали рейса
- `GET /flights/{id}/seat-map` - Карта мест
- `POST /seats/{id}/hold` - Удержание места
- `POST /bookings` - Создание бронирования
- `GET /bookings` - Мои бронирования
- `POST /payments` - Оплата (мок)
- `POST /tickets/{id}/check-in` - Регистрация на рейс
- `GET /tickets/{id}/boarding-pass` - Посадочный талон

### Для сотрудников (STAFF)
- `POST /airports` - Создание аэропорта
- `POST /airplanes` - Создание самолёта (с шаблоном мест)
- `POST /flights` - Создание рейса
- `PATCH /flights/{id}` - Обновление статуса рейса (SCHEDULED, DELAYED, BOARDING, DEPARTED, ARRIVED, CANCELLED, COMPLETED)
- `GET /bookings/all` - Все бронирования в системе
- `GET /flights/{id}/bookings` - Бронирования по конкретному рейсу
- `POST /announcements` - Создание объявления для пассажиров

**ВАЖНО:** Все STAFF эндпоинты защищены JWT и проверкой роли. Пассажиры не имеют доступа к этим функциям.

### Общие
- `GET /airports` - Список аэропортов
- `GET /announcements` - Список объявлений

## 🔐 Как работает аутентификация

1. Пользователь регистрируется или входит через `/register` или `/login`
2. Сервер возвращает JWT токен
3. Flutter сохраняет токен
4. При каждом запросе Flutter отправляет токен в заголовке `Authorization: Bearer <токен>`
5. Сервер проверяет токен и определяет пользователя

## ⚠️ Типичные ошибки новичков

1. **Не активировали виртуальное окружение**
   - Решение: выполните `.\venv\Scripts\Activate.ps1`

2. **Ошибка "Module not found"**
   - Решение: убедитесь, что установили зависимости: `pip install -r requirements.txt`

3. **Ошибка при запуске seed.py**
   - Решение: сначала запустите сервер один раз, чтобы создались таблицы, или запустите seed.py ещё раз

4. **Порт 8000 уже занят**
   - Решение: закройте другое приложение на порту 8000 или используйте другой порт: `uvicorn main:app --reload --port 8001`

5. **База данных не создаётся**
   - Решение: убедитесь, что у вас есть права на запись в папку backend

## 🐛 Отладка

Если что-то не работает:

1. Проверьте, что сервер запущен (должна быть строка "Uvicorn running on http://127.0.0.1:8000")
2. Откройте http://127.0.0.1:8000/docs и попробуйте вызвать эндпоинт там
3. Проверьте логи в терминале - там будут ошибки
4. Убедитесь, что база данных создана (файл `aviasales.db` должен быть в папке backend)

## 📝 Что дальше?

После запуска backend:
1. Убедитесь, что сервер работает (откройте /docs)
2. Протестируйте вход: `POST /login` с данными `passenger@test.com` / `password123`
3. Переходите к настройке Flutter приложения

## 🔗 Подключение Flutter

Flutter приложение будет обращаться к API по адресу:
- **Локально**: `http://127.0.0.1:8000`
- **Эмулятор Android**: `http://10.0.2.2:8000`
- **Реальный телефон**: `http://<ваш-ip-адрес>:8000`

Узнать свой IP адрес в Windows:
```powershell
ipconfig
```
Ищите "IPv4 Address" в разделе вашего сетевого адаптера.

