# Mobile - Airline Booking Flutter App

Это мобильное приложение для системы бронирования авиабилетов на Flutter.

## 📋 Что это такое?

Flutter приложение - это клиентская часть проекта, которая:
- Показывает интерфейс пользователю
- Отправляет запросы к backend API
- Отображает данные (рейсы, места, бронирования)
- Обрабатывает действия пользователя (поиск, бронирование, оплата)

## 🛠️ Технологии

- **Flutter** - фреймворк для создания мобильных приложений
- **Dart** - язык программирования Flutter
- **HTTP** - для запросов к API
- **SharedPreferences** - для хранения JWT токена
- **Material Design** - дизайн интерфейса

## 📦 Установка и запуск

### Шаг 1: Установите Flutter

1. Скачайте Flutter с официального сайта: https://flutter.dev/docs/get-started/install/windows
2. Распакуйте архив в папку (например, `C:\flutter`)
3. Добавьте Flutter в PATH (переменные окружения)

Проверьте установку:
```powershell
flutter --version
```

### Шаг 2: Установите Android Studio

1. Скачайте Android Studio: https://developer.android.com/studio
2. Установите Android Studio
3. Откройте Android Studio → More Actions → SDK Manager
4. Установите Android SDK (API 33 или выше)
5. Создайте виртуальное устройство (AVD): Tools → Device Manager → Create Device

### Шаг 3: Проверьте настройки Flutter

```powershell
flutter doctor
```

Исправьте все проблемы, которые покажет команда.

### Шаг 4: Перейдите в папку mobile

```powershell
cd mobile
```

### Шаг 5: Установите зависимости

```powershell
flutter pub get
```

Эта команда установит все библиотеки из `pubspec.yaml`.

### Шаг 6: Настройте URL API

Откройте файл `lib/api_service.dart` и измените `baseUrl`:

- **Для эмулятора Android**: `http://10.0.2.2:8000` (уже установлено)
- **Для реального телефона**: `http://<ваш-ip-адрес>:8000`
  - Узнать IP: в PowerShell выполните `ipconfig`
  - Найдите "IPv4 Address" вашего сетевого адаптера

### Шаг 7: Убедитесь, что backend запущен

Backend должен работать на `http://127.0.0.1:8000`.

Проверьте: откройте в браузере http://127.0.0.1:8000/docs

### Шаг 8: Запустите приложение

```powershell
flutter run
```

Или запустите из Android Studio:
1. Откройте папку `mobile` в Android Studio
2. Выберите устройство (эмулятор или реальный телефон)
3. Нажмите кнопку "Run" (зелёная стрелка)

## 📁 Структура проекта

```
mobile/
├── lib/
│   ├── main.dart              # Точка входа в приложение
│   ├── api_service.dart       # Все HTTP запросы к API
│   ├── models.dart            # Модели данных
│   └── screens/               # Все экраны приложения
│       ├── login_screen.dart
│       ├── register_screen.dart
│       ├── flight_search_screen.dart
│       ├── flight_details_screen.dart
│       ├── booking_screen.dart
│       ├── my_trips_screen.dart
│       └── announcements_screen.dart
├── pubspec.yaml               # Конфигурация и зависимости
└── README.md                  # Этот файл
```

## 📱 Экраны приложения

1. **Login Screen** - Вход в систему
2. **Register Screen** - Регистрация нового пользователя
3. **Flight Search Screen** - Поиск рейсов по аэропортам и дате
4. **Flight Details Screen** - Детали рейса и выбор места
5. **Booking Screen** - Бронирование и оплата
6. **My Trips Screen** - Список моих бронирований
7. **Announcements Screen** - Объявления от авиакомпании

## 🔌 Как работает связь с backend

1. **API Service** (`api_service.dart`) - это "телефон" для связи с сервером
2. Все запросы идут через этот файл
3. JWT токен сохраняется локально и добавляется автоматически к каждому запросу
4. Ответы от API превращаются в объекты Dart (модели)

Пример:
```dart
// В api_service.dart
final flights = await ApiService.searchFlights(
  fromCode: "SVO",
  toCode: "LED",
);

// Flutter получает список рейсов и показывает их на экране
```

## ⚠️ Типичные ошибки новичков

1. **Ошибка "Connection refused"**
   - Решение: убедитесь, что backend запущен
   - Проверьте URL в `api_service.dart`
   - Для эмулятора используйте `10.0.2.2:8000`
   - Для реального телефона используйте IP вашего компьютера

2. **Ошибка "Failed to load airports"**
   - Решение: проверьте, что backend работает
   - Откройте http://127.0.0.1:8000/docs в браузере

3. **Ошибка "Unauthorized"**
   - Решение: войдите в систему заново
   - Токен мог истечь или быть неверным

4. **Приложение не запускается**
   - Решение: выполните `flutter clean` и `flutter pub get`
   - Проверьте, что эмулятор запущен или телефон подключён

5. **Ошибка компиляции**
   - Решение: убедитесь, что все зависимости установлены: `flutter pub get`
   - Проверьте версию Flutter: `flutter --version` (должна быть 3.0+)

## 🔧 Настройка для реального телефона

1. Узнайте IP адрес вашего компьютера:
   ```powershell
   ipconfig
   ```

2. Измените `baseUrl` в `api_service.dart`:
   ```dart
   static const String baseUrl = 'http://192.168.1.100:8000'; // Ваш IP
   ```

3. Убедитесь, что телефон и компьютер в одной Wi-Fi сети

4. На телефоне разрешите установку из неизвестных источников (для debug версии)

5. Подключите телефон по USB и выполните:
   ```powershell
   flutter devices
   flutter run
   ```

## 🧪 Тестовые данные

После запуска backend с `seed.py`, используйте:

**Пассажир:**
- Email: `passenger@test.com`
- Пароль: `password123`

**Рейсы для тестирования:**
- SU100: Москва (SVO) → Санкт-Петербург (LED)
- SU101: Санкт-Петербург (LED) → Москва (SVO)
- SU200: Москва (SVO) → Сочи (AER)
- SU300: Сочи (AER) → Краснодар (KRR)

## 📝 Что дальше?

1. Запустите backend (см. README в папке backend)
2. Запустите Flutter приложение
3. Зарегистрируйтесь или войдите
4. Заполните профиль (обязательно перед бронированием)
5. Найдите рейс и забронируйте место
6. Оплатите и пройдите check-in

## 🐛 Отладка

Если что-то не работает:

1. Проверьте логи Flutter в терминале
2. Проверьте, что backend работает (откройте /docs)
3. Проверьте URL в `api_service.dart`
4. Убедитесь, что токен сохраняется (проверьте SharedPreferences)
5. Используйте `flutter run -v` для подробных логов

## 💡 Полезные команды

```powershell
# Очистить кэш
flutter clean

# Установить зависимости
flutter pub get

# Проверить устройства
flutter devices

# Запустить приложение
flutter run

# Собрать APK
flutter build apk
```

## 🔗 Связь с backend

Приложение общается с backend через REST API:
- Все запросы идут через `api_service.dart`
- JWT токен передаётся в заголовке `Authorization: Bearer <токен>`
- Данные передаются в формате JSON

Успешной разработки! 🚀

