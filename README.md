# Airline Booking & Operations System

Полная система бронирования авиабилетов с backend на FastAPI и мобильным приложением на Flutter.

## 📋 Что это?

Это учебный проект, который демонстрирует:
- Backend API на FastAPI (Python)
- Мобильное приложение на Flutter (Dart)
- Полный цикл бронирования: поиск → выбор места → оплата → check-in

## 🏗️ Архитектура проекта

```
aviasales/
├── backend/          # FastAPI сервер
│   ├── main.py      # Все API эндпоинты
│   ├── models.py    # Модели базы данных
│   ├── schemas.py   # Схемы валидации
│   ├── auth.py      # Аутентификация
│   ├── database.py  # Подключение к БД
│   └── seed.py      # Тестовые данные
│
└── mobile/          # Flutter приложение
    ├── lib/
    │   ├── main.dart
    │   ├── api_service.dart  # HTTP запросы
    │   ├── models.dart       # Модели данных
    │   └── screens/          # Все экраны
    └── pubspec.yaml
```

## 🚀 БЫСТРЫЙ СТАРТ

### ШАГ 1: Запуск Backend

1. **Откройте PowerShell** (Win + X → PowerShell)

2. **Перейдите в папку backend:**
   ```powershell
   cd backend
   ```

3. **Создайте виртуальное окружение:**
   ```powershell
   python -m venv venv
   ```

4. **Активируйте виртуальное окружение:**
   ```powershell
   .\venv\Scripts\Activate.ps1
   ```
   
   Если появилась ошибка про политику выполнения:
   ```powershell
   Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
   ```
   Затем повторите активацию.

5. **Установите зависимости:**
   ```powershell
   pip install -r requirements.txt
   ```

6. **Создайте тестовые данные:**
   ```powershell
   python seed.py
   ```

7. **Запустите сервер:**
   ```powershell
   uvicorn main:app --reload
   ```

   Сервер запустится на **http://127.0.0.1:8000**

8. **Проверьте работу:**
   - Откройте в браузере: http://127.0.0.1:8000/docs
   - Вы должны увидеть документацию API

✅ **Backend готов!** Оставьте это окно PowerShell открытым.

---

### ШАГ 2: Запуск Flutter приложения

1. **Откройте НОВОЕ окно PowerShell** (не закрывайте то, где запущен backend)

2. **Проверьте установку Flutter:**
   ```powershell
   flutter --version
   ```
   
   Если Flutter не установлен:
   - Скачайте: https://flutter.dev/docs/get-started/install/windows
   - Добавьте в PATH
   - Установите Android Studio и Android SDK

3. **Перейдите в папку mobile:**
   ```powershell
   cd mobile
   ```

4. **Установите зависимости:**
   ```powershell
   flutter pub get
   ```

5. **Настройте URL API:**
   
   Откройте файл `mobile/lib/api_service.dart` и проверьте строку:
   ```dart
   static const String baseUrl = 'http://10.0.2.2:8000';
   ```
   
   - Для **эмулятора Android**: оставьте `10.0.2.2:8000` ✅
   - Для **реального телефона**: замените на `http://<ваш-ip>:8000`
     - Узнать IP: `ipconfig` в PowerShell
     - Найдите "IPv4 Address"

6. **Запустите эмулятор Android:**
   - Откройте Android Studio
   - Tools → Device Manager
   - Запустите виртуальное устройство

7. **Запустите приложение:**
   ```powershell
   flutter run
   ```

✅ **Приложение запущено!**

---

## 🧪 Тестирование

### Учётные данные:

**Пассажир:**
- Email: `passenger@test.com`
- Пароль: `password123`

**Сотрудник:**
- Email: `staff@test.com`
- Пароль: `password123`

### Сценарий использования:

1. **Войдите** как пассажир
2. **Заполните профиль** (обязательно!)
   - Имя, фамилия, дата рождения, паспорт
3. **Найдите рейс:**
   - Откуда: `SVO` (Москва)
   - Куда: `LED` (Санкт-Петербург)
   - Дата: сегодня
4. **Выберите рейс** и **места**
5. **Забронируйте** и **оплатите**
6. **Пройдите check-in** в разделе "Мои поездки"

---

## 📚 Подробная документация

- **Backend:** см. `backend/README.md`
- **Mobile:** см. `mobile/README.md`

---

## ⚠️ Типичные проблемы

### Backend не запускается

1. Проверьте Python: `python --version` (должен быть 3.10+)
2. Убедитесь, что виртуальное окружение активировано (должно быть `(venv)` в начале строки)
3. Проверьте, что все зависимости установлены: `pip list`
4. Убедитесь, что порт 8000 свободен

### Flutter не подключается к backend

1. **Для эмулятора:** используйте `10.0.2.2:8000`
2. **Для реального телефона:**
   - Убедитесь, что телефон и компьютер в одной Wi-Fi сети
   - Используйте IP адрес компьютера (не 127.0.0.1!)
   - Проверьте, что брандмауэр не блокирует порт 8000
3. Проверьте, что backend запущен: откройте http://127.0.0.1:8000/docs

### Ошибка "Connection refused"

- Backend не запущен → запустите `uvicorn main:app --reload`
- Неправильный URL → проверьте `api_service.dart`
- Разные сети → убедитесь, что устройства в одной сети

### Ошибка "Unauthorized"

- Токен истёк → войдите заново
- Неправильные учётные данные → используйте `passenger@test.com` / `password123`

---

## 🎯 Что дальше?

1. ✅ Запустите backend
2. ✅ Запустите Flutter приложение
3. ✅ Протестируйте все функции
4. 📖 Изучите код - он написан просто и с комментариями
5. 🔧 Модифицируйте под свои нужды

---

## 📝 Структура проекта

### Backend (FastAPI)

- `main.py` - все API эндпоинты
- `models.py` - таблицы базы данных
- `schemas.py` - валидация данных
- `auth.py` - JWT аутентификация
- `database.py` - подключение к SQLite
- `seed.py` - создание тестовых данных

### Mobile (Flutter)

- `main.dart` - точка входа
- `api_service.dart` - все HTTP запросы
- `models.dart` - модели данных
- `screens/` - все экраны приложения

---

## 🎓 Для новичков

Этот проект создан специально для обучения:
- ✅ Простой код без сложных паттернов
- ✅ Подробные комментарии
- ✅ Пошаговые инструкции
- ✅ Готовые тестовые данные

**Не бойтесь экспериментировать!** Изменяйте код, добавляйте функции, учитесь на ошибках.

---

## 📞 Поддержка

Если что-то не работает:
1. Проверьте логи в терминале
2. Убедитесь, что все шаги выполнены
3. Проверьте документацию в `backend/README.md` и `mobile/README.md`

---

**Успешной разработки! 🚀**

